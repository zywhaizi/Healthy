---
description: MVVM 架构规范。所有 Swift 代码必须遵守 MVVM 架构。
globs: "**/*.swift"
alwaysApply: true
---

# MVVM 架构规范

## 架构层次

```
View 层 (UIViewController)
  ↓ (持有 ViewModel，手动订阅 @Published 属性)
ViewModel 层 (@Published 属性)
  ↓ (调用方法)
Model 层 (HDHealthDataModel.shared()   ← OC 单例)
  ↓ (读写数据)
本地存储
```

## 各层职责与禁止事项

### Model 层（`HDHealthDataModel.shared()`）
- 职责：数据存储、业务逻辑计算、持久化
- 写入只能用指定方法：`addWater` / `addSteps` / `addMood`
- ❌ 禁止在 Model 中写 UI 代码
- ❌ 禁止在 Model 中调用 ViewController

### ViewModel 层
- 职责：处理用户交互、调用 Model 方法、提供 UI 所需数据
- 使用 `@Published` 属性驱动 UI
- ❌ 禁止在 ViewModel 中直接修改 Model 属性（必须调用 Model 方法）
- ❌ 禁止在 ViewModel 中写 UI 代码
- ❌ 禁止在 ViewModel 中持有 ViewController 引用

### View / ViewController 层
- 职责：展示数据、接收用户交互、实现 `applyTheme`
- ❌ 禁止在 View 中直接访问 `HDHealthDataModel.shared()`
- ❌ 禁止在 View 中写业务逻辑或数据计算

## 数据流向（必须遵守）

```
用户操作 → ViewController 调用 ViewModel 方法
  → ViewModel 调用 HDHealthDataModel 方法
  → Model 修改数据
  → @Published 触发 → UI 自动刷新
```
