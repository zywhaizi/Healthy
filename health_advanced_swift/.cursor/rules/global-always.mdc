---
description: HealthDashboard 全局 ObjC 编码规范与架构约束，所有对话强制生效
globs: "**/*"
alwaysApply: true
---

# HealthDashboard · 全局规范

## 命名约定

- 所有自定义类必须使用 `HD` 前缀：`HDXxxViewController`、`HDXxxView`、`HDXxxModel`
- ViewController 命名：`HD{PageName}ViewController`
- View 命名：`HD{Component}View`
- Protocol 命名：`HD{Name}Delegate`
- 私有属性用下划线前缀：`_propertyName`
- 常量用 `kHD` 前缀：`kHDDefaultStepsGoal`

## 架构约束（MVC）

- **Model**: 数据逻辑只在 Model 层，VC 不持有原始数据，统一通过 `[HDHealthDataModel shared]` 访问
- **View**: 自定义 View 不直接访问 Model，数据由 VC 传入
- **Controller**: VC 负责协调 View 和 Model，不写业务计算逻辑
- 禁止在 View 中直接调用 `[HDHealthDataModel shared]`

## 代码风格

- 使用 `NS_ASSUME_NONNULL_BEGIN / END` 包裹头文件
- 属性声明顺序：`nonatomic` → `strong/weak/assign` → `readonly/copy`
- 方法实现顺序：`init` → `lifecycle` → `public methods` → `private methods`
- 注释使用中文，关键方法必须有 `///` 文档注释
- 每个文件顶部保留创建信息注释块

## 主题系统

- 所有 ViewController 必须实现 `applyTheme` 方法
- 主题状态通过 `[HDHealthDataModel shared].isDarkMode` 读取
- 颜色不得硬编码，使用语义化颜色（参考 design-system Skill）
- Dark Mode 切换后必须调用 `applyTheme` 刷新所有 UI

## 数据操作

- 写入数据只能用 Model 提供的方法：`addWater:`、`addSteps:`、`addMood:`
- 读取进度用 readonly 属性：`stepsProgress`、`waterProgress`
- 不得在 VC 中直接修改 `HDHealthDataModel` 的 readwrite 属性（除配置类属性）

## 禁止事项

- 禁止引入第三方库（需人工确认后方可）
- 禁止修改 `Info.plist` 关键字段（Bundle ID、权限描述等）
- 禁止在主线程做耗时操作
- 禁止使用 `performSelector` 绕过类型检查
- 禁止硬编码字符串（用常量或 Localizable.strings）
- 禁止删除任何现有的 `applyTheme` 实现

## Guardrails（安全护栏）

以下操作必须**停下来询问用户**，不得自行执行：

- 删除任何 `.h` / `.m` / `.storyboard` / `.plist` 文件
- 执行 `rm -rf` 或任何批量删除命令
- 修改 `project.pbxproj`
- 读取或修改 `.env`、证书（`.p12`）、`.mobileprovision` 文件
- 引入新的 CocoaPods 依赖或 SPM 包

## Quality Gate（质量门禁）

每次完成代码修改后，Agent **必须自查**以下项目再报告完成：

- [ ] 新增类使用了 `HD` 前缀
- [ ] 头文件包含 `NS_ASSUME_NONNULL_BEGIN/END`
- [ ] 没有硬编码颜色值
- [ ] 没有使用 `performSelector`
- [ ] 修改了数据相关代码 → 确认通过 Model 方法写入
- [ ] 修改了 UI 相关代码 → 确认实现了 `applyTheme`


