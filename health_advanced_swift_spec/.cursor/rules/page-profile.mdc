---
description: 个人中心页面专属约束，修改 HDProfile 相关文件时自动生效
globs: "**/HDProfile*"
alwaysApply: false
---

# Profile 页面约束

## 禁止事项

- 禁止在此页面直接添加健康数据（录入功能在 QuickAdd）
- 禁止硬编码目标值（从 Model 读取）
- 禁止硬编码颜色值（使用语义色）
- 禁止直接修改 `sleepHours` 或 `moodRecords`

## 必须遵守

- 必须实现 `applyTheme` 方法响应主题切换
- 必须通过 `HDHealthDataModel.shared.isDarkMode` 读取主题状态
- 必须订阅 `HDHealthDataModel.shared.$isDarkMode` 响应主题变化
- 目标设置必须通过 `stepsGoal` 和 `waterGoalML` 属性读写
- 数据统计只能展示，不能修改（`moodRecords`、`sleepHours`）

## 质量门禁

- [ ] 没有硬编码颜色值
- [ ] 实现了 `applyTheme` 方法
- [ ] 主题状态读取与订阅使用 `HDHealthDataModel.shared`
- [ ] 没有直接修改 Model 只读数据
- [ ] 目标值从 Model 读取并可写回配置属性
