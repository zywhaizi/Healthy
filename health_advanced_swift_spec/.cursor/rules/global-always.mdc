# HealthDashboard · 全局规范

> 当前技术栈：**Swift + UIKit + MVVM + Combine**

## 命名约定

- 所有自定义类必须使用 `HD` 前缀：`HDXxxViewController`、`HDXxxView`、`HDXxxViewModel`
- ViewController 命名：`HD{PageName}ViewController`
- ViewModel 命名：`HD{PageName}ViewModel`
- View 命名：`HD{Component}View`
- Protocol 命名：`HD{Name}Delegate`
- 私有属性用下划线前缀：`_propertyName`
- 常量用 `kHD` 前缀：`kHDDefaultStepsGoal`

## 架构约束（MVVM）

- **Model**: 数据逻辑只在 Model 层，统一通过 `HDHealthDataModel.shared` 访问
- **ViewModel**: 持有 `@Published` 属性驱动 UI，调用 Model 方法写入数据
- **View/ViewController**: 持有 ViewModel，通过 Combine 订阅 `@Published` 属性更新 UI
- 禁止在 View 中直接调用 `HDHealthDataModel.shared`
- 禁止在 ViewController 中直接写业务计算逻辑

## 代码风格

- 使用 4 空格缩进，每行最多 120 字符
- 使用 `// MARK: -` 分组方法
- 方法实现顺序：`init` → `lifecycle` → `public methods` → `private methods`
- 注释使用中文，关键方法必须有文档注释
- 每个文件顶部保留创建信息注释块

## 主题系统

- 所有 ViewController 必须实现 `applyTheme` 方法
- 主题状态通过 `HDHealthDataModel.shared.isDarkMode` 读取
- 主题变化监听：订阅 `HDHealthDataModel.shared.$isDarkMode`（Combine，禁止用 NSNotification）
- 颜色不得硬编码，使用系统语义色（`.systemBackground`、`.label` 等）

## 数据操作

- 写入数据只能用 Model 提供的方法：`addWater()`、`addSteps()`、`addMood()`
- 读取进度用 readonly 属性：`stepsProgress`、`waterProgress`
- 不得在 ViewModel 中直接修改 `HDHealthDataModel` 的 readwrite 属性（除 `isDarkMode`、`stepsGoal`、`waterGoalML` 配置类属性）

## Combine 绑定规范

- 所有订阅必须用 `.store(in: &cancellables)` 保存
- 订阅中引用 `self` 必须用 `[weak self]`
- 主线程更新 UI 必须加 `.receive(on: DispatchQueue.main)`

## 禁止事项

- 禁止引入第三方库（需人工确认后方可）
- 禁止修改 `Info.plist` 关键字段（Bundle ID、权限描述等）
- 禁止在主线程做耗时操作
- 禁止硬编码字符串（用常量）
- 禁止硬编码颜色值（使用语义色）
- 禁止删除任何现有的 `applyTheme` 实现
- 禁止在 Combine 订阅中强引用 `self`（必须 `[weak self]`）

## Guardrails（安全护栏）

以下操作必须**停下来询问用户**，不得自行执行：

- 删除任何 `.swift` / `.storyboard` / `.plist` 文件
- 执行 `rm -rf` 或任何批量删除命令
- 修改 `project.pbxproj`
- 读取或修改 `.env`、证书（`.p12`）、`.mobileprovision` 文件
- 引入新的 CocoaPods 依赖或 SPM 包

## Quality Gate（质量门禁）

每次完成代码修改后，Agent **必须自查**以下项目再报告完成：

- [ ] 新增类使用了 `HD` 前缀
- [ ] 没有硬编码颜色值
- [ ] 没有硬编码字符串
- [ ] ViewModel 使用了 `@Published` 驱动 UI
- [ ] Combine 订阅使用了 `[weak self]` 且存入 `cancellables`
- [ ] 修改了数据相关代码 → 确认通过 Model 方法写入
- [ ] 修改了 UI 相关代码 → 确认实现了 `applyTheme`
