---
description: 运动模块页面约束。包括运动类型选择、计时器、设置、统计等页面的规范。
globs: "HealthDashboard/Controllers/HDExercise*.{h,m}"
alwaysApply: false
---

# Exercise 页面约束

## 禁止事项

- 禁止在此模块直接修改 `HDHealthDataModel` 的数据（如 `model.exerciseMinutes = 30`）
- 禁止在 View 中直接访问 `[HDHealthDataModel shared]`
- 禁止硬编码颜色值（使用语义色）
- 禁止在计时器中做耗时操作（使用后台线程）
- 禁止修改运动数据而不通知 Dashboard 刷新

## 必须遵守

- 必须在 `viewWillAppear:` 中调用 `refreshData()` 和 `applyTheme()`
- 必须实现 `applyTheme` 方法响应主题切换
- 必须通过 `HDHealthDataModel.shared` 的 readonly 属性读取数据
- 运动数据写入必须通过 Model 提供的方法（如 `addExercise:duration:calories:`）
- 计时器必须使用 `NSTimer` 或 `dispatch_source_create` 实现，不得阻塞主线程
- 运动完成后必须调用 Delegate 回调通知 Dashboard 刷新

## 页面结构

```
Tab 3: HDExerciseTypeViewController      // 运动类型选择
  ↓ (选择类型后)
  HDExerciseTimerViewController          // 运动计时
  ↓ (完成后)
  HDExerciseSummaryViewController        // 运动统计
  
侧边栏/设置:
  HDExerciseSettingViewController        // 运动目标设置
```

## 数据流向

```
用户选择运动类型 → 进入计时页面 → 计时完成 → 保存数据到 Model
→ 调用 Delegate 回调 → Dashboard 刷新显示
```

## 质量门禁

- [ ] 数据读取只通过 Model 的 readonly 属性
- [ ] 数据写入通过 Model 方法
- [ ] 没有硬编码颜色值
- [ ] 实现了 `applyTheme` 方法
- [ ] 没有在 View 中访问 Model
- [ ] 计时器不阻塞主线程
- [ ] 调用了 Delegate 回调
