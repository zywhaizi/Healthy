---
description: 运动模块页面约束。包括运动类型、设置、计时、总结页面的规范。
globs: "HealthDashboard/Controllers/HDExercise*.swift"
alwaysApply: false
---

# Exercise 页面约束

## 禁止事项

- 禁止在此模块直接修改 `HDHealthDataModel` 业务数据（必须通过 Model 方法）
- 禁止在 View 中直接访问 `HDHealthDataModel.shared`
- 禁止硬编码颜色值（使用语义色）
- 禁止在计时器回调中执行耗时操作

## 必须遵守

- 各页面必须实现 `applyTheme` 方法
- 必须订阅 `HDHealthDataModel.shared.$isDarkMode` 响应主题切换
- 目标设置通过 `targetRunDistanceKM`、`targetRunMinutes` 读写
- 计时页面结束时必须通过 `saveExerciseRecord(_:)` 保存记录
- 计时器必须在页面结束或暂停时 `invalidate()`，避免泄漏

## 页面结构

```
Tab 1: HDExerciseTypeViewController      // 运动类型选择
  ↓ (选择目标跑)
  HDExerciseSettingViewController        // 目标设置
  ↓
  HDExerciseTimerViewController          // 运动计时
  ↓
  HDExerciseSummaryViewController        // 运动总结
```

## 数据流向

```
用户选择运动类型 → 设置目标（可选）→ 开始计时
→ 结束运动构建 HDExerciseRecord
→ HDHealthDataModel.shared.saveExerciseRecord(record)
→ 进入总结页展示
```

## 质量门禁

- [ ] globs 覆盖 `HDExercise*.swift`
- [ ] 主题订阅使用 Combine `$isDarkMode`
- [ ] 运动数据写入通过 `saveExerciseRecord(_:)`
- [ ] 计时器生命周期处理正确（invalidate）
- [ ] 没有硬编码颜色值
