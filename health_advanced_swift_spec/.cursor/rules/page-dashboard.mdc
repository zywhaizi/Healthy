---
description: 仪表盘页面专属约束，修改 HDDashboard 相关文件时自动生效
globs: "**/HDDashboard*"
alwaysApply: false
---

# Dashboard 页面约束

## 禁止事项

- 禁止在 VC 中直接修改 `HDHealthDataModel` 的数据（如 `model.todaySteps = 1000`）
- 禁止在 View 中直接访问 `HDHealthDataModel.shared`
- 禁止硬编码颜色值（使用语义色）
- 禁止在 `refreshData` 中做耗时操作
- 禁止在 ViewController 中写业务计算逻辑（放在 ViewModel）

## 必须遵守

- 必须持有 `HDDashboardViewModel` 实例，通过 Combine 订阅 `@Published` 属性驱动 UI
- 必须通过 `HDHealthDataModel.shared` 的 readonly 属性读取数据（`stepsProgress`、`waterProgress` 等）
- 必须实现 `applyTheme` 方法响应主题切换
- 主题变化监听：订阅 `HDHealthDataModel.shared.$isDarkMode`（Combine）
- Dashboard 必须 conform `HDQuickAddDelegate`，在 `quickAddDidUpdateData()` 中调用 `viewModel.refreshData()`
- FAB 按钮点击后 present `HDQuickAddViewController`，并设置 `vc.delegate = self`
- 卡片间距：16pt，圆角：12pt，内边距：16pt

## 质量门禁

- [ ] 持有 ViewModel，通过 Combine 驱动 UI
- [ ] 数据读取只通过 Model 的 readonly 属性（在 ViewModel 中）
- [ ] 没有硬编码颜色值
- [ ] 实现了 `applyTheme` 方法
- [ ] conform 了 `HDQuickAddDelegate`
- [ ] FAB 设置了 `vc.delegate = self`
- [ ] 没有在 View 中访问 Model
