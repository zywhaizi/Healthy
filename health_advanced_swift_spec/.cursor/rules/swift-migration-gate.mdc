---
description: Swift 代码质量门禁。聚焦编译零报错、架构合规、功能可用。
globs:
  - "HealthDashboard/**/*.swift"
alwaysApply: false
---

# Swift 代码质量门禁

> 目标：代码 **0 编译错误、架构合规、功能正常**。

## 适用范围

以下任一操作必须启用本门禁：
- 新增或修改 Swift ViewController / ViewModel / View
- 修改数据写入逻辑
- 修改主题切换逻辑
- 修改 Delegate 回调链路

---

## 硬约束（必须）

### 编译约束
- 不允许带着编译错误提交或报告完成
- 修改后必须确认 0 error、0 关键 warning

### 架构约束
- 数据写入必须通过 `HDHealthDataModel` 方法：`addWater` / `addSteps` / `addMood`
- 禁止在 View 中直接访问 `HDHealthDataModel.shared()`
- 禁止在 ViewController 中写业务计算逻辑（放 ViewModel）
- 所有 ViewController 必须实现 `applyTheme`
- 不得删除已有 `applyTheme` 实现

### 安全约束
- 禁止修改 `project.pbxproj`
- 禁止引入第三方依赖
- 禁止硬编码颜色值（使用系统语义色）
- 禁止硬编码字符串（使用常量）

---

## 六项验收（全部通过才可报告完成）

- [ ] A. 编译通过：0 error
- [ ] B. 页面可实例化：ViewController 可正常创建
- [ ] C. 生命周期正常：`viewDidLoad`、`viewWillAppear` 可执行
- [ ] D. 主题正常：`applyTheme` 生效，深浅色切换正常
- [ ] E. 数据链路正常：读写符合 `HDHealthDataModel` 约束
- [ ] F. 回调正常：Delegate 回传链路可用（QuickAdd/Exercise → Dashboard）

---

## 输出模板（每次功能修改必须自查并输出）

````md
[修改对象]
- 页面：HDxxxViewController
- 改动范围：UI / 数据 / 主题 / 回调

[六项验收]
- A 编译：✅/❌
- B 实例化：✅/❌
- C 生命周期：✅/❌
- D 主题：✅/❌
- E 数据链路：✅/❌
- F 委托回调：✅/❌

[遗留风险]
- 无 / 列出风险项
````
