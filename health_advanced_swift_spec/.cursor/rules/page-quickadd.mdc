---
description: 快速录入页面专属约束，修改 HDQuickAdd 相关文件时自动生效
globs: "**/HDQuickAdd*"
alwaysApply: false
---

# QuickAdd 页面约束

> QuickAdd 是 **FAB 弹窗**（底部弹出），不是独立 Tab。
> 入口：`HDDashboardViewController` 右下角 FAB 按钮 → `present(HDQuickAddViewController)`

## 禁止事项

- 禁止在此 VC 中展示历史数据（展示在 Dashboard/Profile）
- 禁止录入完成前 dismiss（必须先回调 delegate，再 dismiss）
- 禁止直接修改 Model 属性（必须通过 `model.addWater()`、`model.addSteps()`、`model.addMood()` 方法）
- 禁止忘记调用 Delegate 回调
- delegate 必须声明为 `weak`（防止循环引用）
- 禁止硬编码颜色值

## 必须遵守

- 必须通过 ViewModel 调用 Model 方法写入数据
- 必须在动画完成的 completion 闭包中**先**调用 `delegate?.quickAddDidUpdateData()`，**再** dismiss
- 必须实现 `applyTheme` 方法
- `delegate` 属性必须声明为 `weak var delegate: HDQuickAddDelegate?`
- `HDQuickAddDelegate` 协议必须标注 `@objc`（兼容 OC 侧 Dashboard conform）

## 录入规格（当前实现）

- 喝水：固定 +200ml，无需用户输入
- 步数：滑块范围 500～10000 步
- 心情：5 个 emoji 按钮，对应 level 1～5

## dismiss 时序（必须遵守）

```swift
// ✅ 正确：先回调，再 dismiss
UIView.animate(..., completion: { _ in
    self.delegate?.quickAddDidUpdateData()   // ① 先回调
    self.dismiss(animated: false)            // ② 再 dismiss
})
```

## 质量门禁

- [ ] 数据写入通过 ViewModel → Model 方法
- [ ] dismiss 前已调用 Delegate 回调
- [ ] delegate 声明为 `weak`
- [ ] `HDQuickAddDelegate` 标注了 `@objc`
- [ ] 没有硬编码颜色值
- [ ] 实现了 `applyTheme` 方法
