---
description: TabBar 控制器专属约束，修改 HDTabBar 相关文件时自动生效
globs: "**/HDTabBar*"
alwaysApply: false
---

# TabBar 页面约束

## 当前 Tab 结构（顺序固定，不得随意调整）

```
Tab 0: HDDashboardViewController    // 首页·仪表盘
Tab 1: HDExerciseTypeViewController // 运动模块
Tab 2: HDProfileViewController      // 个人中心
```

> ⚠️ QuickAdd **不是独立 Tab**，已改为 Dashboard 页面右下角 FAB 浮窗弹出。

## 禁止事项

- 禁止在 TabBar 中处理业务逻辑
- 禁止随意调整 Tab 顺序（影响全局导航）
- 禁止修改 Tab 数量（需人工确认）
- 禁止使用硬编码颜色值（使用语义色）
- 禁止在 TabBar 中直接访问 `HDHealthDataModel`

## 必须遵守

- 必须实现 `applyTabBarStyle` 方法响应主题切换
- 必须通过 `HDHealthDataModel.shared.$isDarkMode` 的 Combine 订阅监听主题变化
- TabBar 背景色必须使用 `.systemBackground`（语义色）
- 主题切换时各子 VC 通过自身订阅 `$isDarkMode` 响应，TabBar 不需要遍历通知

## 质量门禁

- [ ] Tab 顺序正确（Dashboard / Exercise / Profile）
- [ ] 实现了 `applyTabBarStyle` 方法
- [ ] 没有硬编码颜色值
- [ ] 主题订阅使用 Combine `$isDarkMode`
- [ ] 没有在 TabBar 中处理业务逻辑
