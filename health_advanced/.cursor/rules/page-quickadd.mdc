---
description: 快速录入页面专属约束，修改 HDQuickAdd 相关文件时自动生效
globs: "**/HDQuickAdd*"
alwaysApply: false
---

# QuickAdd 页面约束

## 禁止事项

- 禁止在此 VC 中展示历史数据（展示在 Dashboard/Profile）
- 禁止录入完成前 dismiss（防止数据丢失）
- 禁止直接修改 Model 数据（必须通过 `addWater:`、`addSteps:`、`addMood:` 方法）
- 禁止忘记调用 Delegate 回调
- delegate 必须声明为 `weak`（防止循环引用）

## 必须遵守

- 必须调用对应 Model 方法写入数据（`addWater:`、`addSteps:`、`addMood:`）
- 必须在数据写入后调用 `[self.delegate quickAddDidUpdateData]`
- 必须进行表单校验（喝水 100-2000ml、步数 1-50000、心情 1-5）
- 必须实现 `applyTheme` 方法

## 表单校验规则

- 喝水：100ml ≤ 输入值 ≤ 2000ml
- 步数：1 ≤ 输入值 ≤ 50000
- 心情：整数 1~5

## 质量门禁

- [ ] 数据写入通过 Model 方法
- [ ] 调用了 Delegate 回调
- [ ] delegate 声明为 weak
- [ ] 表单校验完整
